# Brukervennlighetssjekkliste

| Dato | Build/commit | Scenario | Deltakerprofil | Resultat | Funn | Oppfølging |
|---|---|---|---|---|---|---|
| 2026-08-02 | AUTO-002 working tree | Execute a recommended-policy plan containing one absent new file and one changed existing target, then inspect the terminal result at 900x560 | Automated real SQLite/filesystem execution plus adversarial Qt layout test | passed | Only the new file entered staging/recovery and reached the target; the changed target remained byte-for-byte untouched as an immutable deferred replacement row; the run reported action required with exact deferred count/bytes, localized terminal copy wrapped, and horizontal overflow remained zero | Covered by `tests/integration/sqlite/test_sqlite_run_executor_cycle.py::test_new_files_only_cycle_copies_new_file_and_defers_changed_target`, `tests/integration/sqlite/test_sqlite_runs.py`, `tests/unit/test_initial_backup_planning.py`, and `tests/gui/test_pyside_shell.py::test_jobs_page_labels_deferred_automation_result_as_action_required` |
| 2026-08-02 | UX-005 evidence working tree | Click Run Backup once after a completed backup, first when fresh analysis finds only safe new-file work and then when it finds a replacement requiring review | Automated Qt interaction plus application and Engine Host policy tests | passed | The safe plan queued a new run from the one click; the changed-risk plan did not start and the visible action changed to a separate Start Backup review action; neither path directly started the stale completed plan | Covered by `tests/gui/test_pyside_shell.py::test_established_backup_one_click_runs_fresh_safe_analysis`, `tests/gui/test_pyside_shell.py::test_established_backup_one_click_stops_changed_risk_for_review`, and `tests/unit/test_backup_analysis.py` |
| 2026-08-02 | daily automation working tree | Configure a daily backup in Jobs at 900x560, save it without blocking the Qt thread, deliver a scheduled occurrence, and copy a file created only after schedule configuration | Automated adversarial Qt interaction plus real SQLite/filesystem execution | passed | The localized checkbox, time editor, Save action and reconciliation status remained bounded with zero horizontal overflow; the durable schedule used same-user Windows policy; the trigger queued a fresh safe analysis, the completed run copied the post-schedule file with exact contents, and its occurrence reached terminal `SUCCEEDED` | Covered by `tests/gui/test_pyside_shell.py::test_jobs_workspace_saves_daily_automation_without_clipping`, `tests/integration/sqlite/test_job_scheduling_command.py::test_daily_schedule_command_is_atomic_durable_and_replayable`, and the full suite result of 1,830 passed plus 25 passing subtests |
| 2026-08-01 | `d281891` local unsigned install | Run a sealed backup through the installed Engine Host, SQLite catalog, Robocopy staging and final commit; then inspect the completed activity at 900x560 | Installed Windows executable plus real filesystem and rendered-layout inspection | passed | The 10,794-byte source and target SHA-256 values matched; the run completed with three operations; `Thumbs.db` remained absent with an exact sealed `FILTER_RULE_EXCLUDED` decision; long activity IDs used visible middle ellipses with their complete values retained for tooltips and accessibility; no horizontal overflow was visible | Covered by the installed-runtime proof result at `C:\claude\mediasynch-build-d281891\live-real-copy\result.json`, package smoke status `PASS`, and responsive GUI coverage in `tests/gui/test_pyside_shell.py` |
| 2026-08-01 | pending-target repair working tree | Open a pre-migration-30 active job with one long `REGISTRATION_PENDING` target at 900×560, click **Registrer mål** twice while the command is held, navigate to Settings, switch to English, then complete registration | Automated adversarial Qt interaction plus SQLite/filesystem integration | passed | The action was enabled and localized, exactly one revision-bound command ran off the GUI thread, navigation and language remained responsive, the long target row stayed inside its panel with an ellipsis and tooltip, and the durable repair advanced the active job exactly once | Covered by `tests/gui/test_pyside_shell.py::test_pending_target_registration_stays_responsive_and_bounded` and `tests/integration/sqlite/test_writable_endpoint_registrations.py::test_explicit_ipc_command_repairs_pending_job_without_registration_intent` |
| 2026-08-01 | multi-target execution working tree | Start one sealed source-to-two-target run and drive both target-bound operations through lease, staging, journal, commit, audit and handoff | Automated adversarial SQLite/filesystem integration | passed | Each target received only its bound operation and source bytes; each lease and recovery chain remained independent; both target counters reached success and the run completed only after both handoffs existed | Covered by `tests/integration/sqlite/test_sqlite_run_executor_cycle.py::test_sqlite_run_executor_cycle_completes_bound_operations_for_two_targets` plus planner/binding coverage in `tests/unit/test_initial_backup_planning.py` and `tests/unit/test_run_operation_planning.py` |
| 2026-08-01 | query-cancellation working tree | Hold en Engine Host-statusrespons åpen, erstatt en aktiv same-key GUI-query og lukk en controller mens transporten venter | Automated adversarial Qt interaction plus real Windows named-pipe transport | passed | Den utdaterte readen ble avbrutt uten manuell release, den nyere queryen overtok den eneste workerplassen, ingen stale callback ble brukt, og den virkelige pipe-readen returnerte `InterruptedError` på under ett sekund | Covered by `tests/gui/test_pyside_shell.py`, `tests/unit/test_engine_client.py`, and `tests/integration/ipc/test_win32_named_pipe.py` |
| 2026-08-01 | page-prefetch working tree | Page through Changes, History and History file results on keyset and legacy hosts; stall a speculative History read, change the foreground filter, and release the stale result | Automated adversarial Qt mouse interaction | passed | A ready next page opened without another foreground IPC read; each surface retained at most one prefetched page; the stalled prefetch neither blocked the newer filter nor repainted its stale result | Covered by `tests/gui/test_pyside_shell.py` and `src/mediasync_home/presentation/background_queries.py` |
| 2026-08-01 | virtual-table working tree | Load Changes and History from a simulated million-row source boundary; click rows, page/filter, switch language, block newer reads, and resize between 900×560 and 1200×760 | Automated Qt interaction plus real Windows rendered-layout inspection | passed | Changes and file results retained at most one 200-row keyset page; History retained one 25-row page; no per-row widgets were created; compact secondary columns moved into the selected-row detail, wide columns returned on resize, and every table retained zero horizontal overflow | Covered by `tests/unit/test_virtual_tables.py` and `tests/gui/test_pyside_shell.py`; rendered evidence: `docs/assets/virtual-tables-changes-dark.png`, `docs/assets/virtual-tables-history-compact-dark.png`, and `docs/assets/virtual-tables-history-wide-dark.png` |
| 2026-08-01 | working tree after `266cb15` | Block create/register, check, start and pause commands; duplicate-click each action; navigate, switch language and select job B while job A waits; retry an uncertain start transport outcome | Automated adversarial Qt mouse interaction | passed | Every mutation ran off the GUI thread on one dedicated worker; duplicate clicks produced one submission; navigation and language stayed responsive; job A's late result did not replace job B; retry reused the exact request and idempotency IDs | Covered by `tests/gui/test_pyside_shell.py` and `src/mediasync_home/presentation/background_queries.py` |
| 2026-08-01 | working tree after `c561d3c` | Block status, job detail, Jobs paging, progress, analysis, Changes, History and file audit reads; click navigation, language, filters and newer job/file choices at 900×560 while each read waits | Automated adversarial Qt mouse interaction | passed | One worker remained active; timer polls did not queue; stale job/page/file results never replaced newer context; controls bound to stale data locked while global navigation and language remained responsive; compact scroll areas retained zero horizontal overflow | Covered by `tests/gui/test_pyside_shell.py` and `src/mediasync_home/presentation/background_queries.py` |
| 2026-07-31 | working tree after `3eceb9c` | Start produktinngangen uten argumenter med eksisterende host og med tom isolert state; gjenta med lokal usignert pakke; lukk GUI-et uten å stoppe hosten | Automated Windows process/window/status inspection | passed | Alle oppstarter åpnet et responsive vindu; eksisterende host ble adoptert uten duplikat; ny kilde- og pakkehost svarte `ACCEPTED`; hostens levetid var uavhengig av GUI-et | Covered by `tests/unit/test_bootstrap_roles.py`, `tests/unit/test_process_supervision.py`, `tests/unit/test_launcher_role.py`, and `artifacts/0b/packaged-runtime-smoke-latest.json` |
| 2026-07-31 | working tree after `cddb87c` | Select a long source and target at 900×560, 1000×650, and 1120×700; add three targets, remove the middle target, go back, and create the remaining two-target draft | Automated Qt keyboard/mouse interaction and rendered-layout inspection | passed | All controls responded; long paths wrapped; dashboard/activity horizontal overflow remained zero; the target step stayed open until explicit Continue | Covered by `tests/gui/test_pyside_shell.py`; rendered evidence: `docs/assets/target-selection-dark.png` and `docs/assets/target-selection-light.png` |
| 2026-07-31 | working tree after `e20850e` | Open Jobs, switch between two jobs with different sealed-plan checksums, start the selected plan, and page forward/back at 900×560 | Automated Qt mouse interaction and light/dark rendered-layout inspection | passed | Selection loaded the exact job revision; Start submitted the selected checksum; paging retained bounded offsets; responsive labels and both scroll areas had zero horizontal overflow | Covered by `tests/gui/test_pyside_shell.py`; rendered evidence: `docs/assets/jobs-workspace-dark.png` and `docs/assets/jobs-workspace-light.png` |
| 2026-07-31 | working tree after `69b83fc` | Open History, select a control and backup with distinct identities, refresh, filter by activity/job, page forward/back, and switch language at 900×560 | Automated Qt mouse interaction and real Windows light/dark rendered-layout inspection | passed | Selection remained bound to activity kind plus immutable ID; filters issued bounded Engine Host queries; timing, counts, byte totals and long target IDs wrapped; the History scroll area had zero horizontal overflow | Covered by `tests/gui/test_pyside_shell.py`; rendered evidence: `docs/assets/history-workspace-dark.png` and `docs/assets/history-workspace-light.png` |

Minimumsscenarier følger §8.30 og §21.10. Registrer faktiske observasjoner; en ikke gjennomført test skal stå som `not_run`, aldri `passed`.
