# 0A.3 evidence artifacts

Generated on 2026-07-16 for branch `spike/0a3-recovery-and-paths`.

- `demo-summary.json` records a sanitized marker-validated local lab run for
  short managed objects, fallback replace/version restore, quarantine,
  `ReplaceFileW`, and local source-guard behavior.
- `unittest-output.txt` records:
  `python -m unittest discover -s tests\spikes\0a3_recovery_paths -v`

The artifacts do not contain raw local account names, personal folders, production
NAS paths, real user file names, credentials, or secrets.
